from company_views import (
    company_index, company_detail, company_new,
    company_edit, company_delete )

from venue_views import (
    venue_index, venue_detail, venue_new,
    venue_edit, venue_delete )

from event_views import (
    event_index, event_detail, event_month, ajax_update_event_month,
    event_week, event_day, event_new, event_edit, event_delete )

from general_views import (
    index, suggestions, scene, admin_dashboard )
